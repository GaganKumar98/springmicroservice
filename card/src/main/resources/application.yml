
server:
   port: 9889
spring:
   datasource:
      url: jdbc:h2:mem:testdb
      driverClassName: org.h2.Driver
      username: sa
      password: ''
   h2:
      console:
         enabled: true
   jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
         ddl-auto: update
      show-sql: true
   config:
    import:
    - "application_qa.yml"
    - "application_prod.yml"
    
build:
  version: "2.0"
  
accounts:
  Messsage: "Welcome to Card MS"
  ContactDetails:
    name: "Gagan Kumar"
    email: "gagan@gmail.com"
  onCallSupport:
    -7982010520